<?php


namespace App\Services\HumanResources;


use App\Models\HumanResources\Timecard;
use Carbon\Carbon;

class AppointmentService
{
    //this is unnecessary right not
    public function assignUserToProject($user, $project)
    {
//        $project->users()->attach($user->id);
//        (new TimecardService())->fixUserTimecard($user);
//        $project->appointments()->get()->last()->generatePastAction();
    }
}
